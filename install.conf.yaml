- defaults:
    link:
      relink: true

- clean: ['~']

- create:
    - ~/.config/nvim

- link:
    ~/.config/nvim/:
        if: '[ -x "$(command -v nvim)" ]'
        glob: true
        path: dotfiles/nvim/*.vim
